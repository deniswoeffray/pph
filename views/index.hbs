{{!-- views/index.hbs --}}

{{#> baseLayout }}

    {{#*inline "content-block"}}
        <h1>{{title}}</h1>
        <div id="msg">{{msg}}</div>
        <!--Post vers la création de formulaire-->
        <form action="/test" method="post">
            <button type="submit">Créer un nouveau test</button>
        </form>

        <input type="text" id="testCode">
        <button type="submit" id="goToTestBtn">Afficher test</button>
    {{/inline}}

    {{#*inline "scripts-block"}}
        <script>

            // Au click du bouton de recherche
            $('#goToTestBtn').click(function () {
                // Erreur si champ de recherche vide
                if($('#testCode').val()=="")
                {
                    $('#msg').html("Merci d'entrer un code dans le champ");
                }
                // Get vers la recherche de formulaire
                else
                {
                    window.location.href = '/test/'+$('#testCode').val();
                }
            })
        </script>
    {{/inline}}

{{/baseLayout}}